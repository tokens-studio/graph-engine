// Simple test file to verify the NodePanel components work
import { NodePanel } from './index.js';
import React from 'react';

// This is just a basic test to ensure the component can be imported and rendered
export const TestNodePanel = () => {
  return <NodePanel />;
};
