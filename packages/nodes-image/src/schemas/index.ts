import { SchemaObject } from "@tokens-studio/graph-engine";

export const IMAGE = "https://schemas.tokens.studio/image/image.json";
export const ImageSchema: SchemaObject = {
    $id: IMAGE,
    title: "Image",
    type: "object",
    default: null,
};

