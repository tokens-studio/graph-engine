import { SchemaObject } from '@tokens-studio/graph-engine';

export const FILE = 'https://schemas.tokens.studio/fs/file.json';
export const FileSchema: SchemaObject = {
	$id: FILE,
	title: 'File',
	type: 'object'
};
